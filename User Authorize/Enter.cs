namespace TsunamiApp
{ 
    public class Enter
    {
        private static void UserEnter(string? name, string? pass, string? key)
        {
            //connect JSON DataBase || PostegreSQL
        }
        public void RunUserEnter(string? name, string? pass, string? key)
        {
            UserEnter(name, pass, key); //userInput parameters
        }
    }
}
